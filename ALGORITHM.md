# 미디어 팩트체크는 어떻게 작동할까?
> 학생을 위한 VAPM v4.0 알고리즘 안내서 (5대 검증 행동 + 교사 기준 보정)

이 문서는 우리 웹앱이 **여러분의 체크리스트**와 **AI(Gemini)** 그리고 **교사 기준 보정(calibration)** 을 어떻게 한 데 묶어 미디어 자료를 평가하는지 풀어 설명합니다. 수학을 잘 몰라도 괜찮아요. 모든 수식 옆에는 "이게 무슨 뜻인지"를 친절하게 적어두었습니다.

> **v4.0에서 무엇이 바뀌었나요?**
> 예전(v3.0)에는 베이지안 가중치(μ,σ)·학습률·cold start·신뢰구간·수렴도가 서로 얽혀 있어서, 점수가 왜 그렇게 나오는지 말로 설명하기 어려웠어요. v4.0은 이걸 전부 걷어내고 **"같은 미디어를 교사와 내가 채점한 평균 차이(보정값)를 AI 점수에 더한다"** 는 한 문장으로 단순화했어요. 교육 현장에서 오래 쓰인 **채점자 보정(rater calibration / moderation)** 방식과 똑같은 논리예요.

---

## 1. 한 줄 요약

> "AI가 매긴 1~5점 + (교사와 나의 평균 채점 차이) → 교사 기준에 맞춘 50점."

모델링(기준 다듬기)을 반복할수록 이 "평균 차이(보정값)"가 점점 정확해져서, 내 채점 버릇이 어느 쪽으로 치우쳐 있든 **교사 기준점에 정박된 점수**를 얻게 됩니다. 그리고 5개 행동 각각의 **마스터리(숙달도)** 도 함께 추적돼요.

---

## 2. 왜 5대 검증 행동인가요?

이전 버전(IPFM v2.0)은 **IFCN 5대 강령**을 기준으로 했어요. 그런데 IFCN 강령은 본래 **팩트체커 조직이 지켜야 할 윤리 강령**이지, 학생이 미디어 자료를 평가할 때 직접 수행하는 행동이 아니에요.

VAPM은 **너희가 미디어 자료를 만났을 때 실제로 해야 하는 5가지 검증 행동**으로 재설계했어요. 각 항목이 "이렇게 행동해보자"라는 구체적 가이드예요.

| 코드 | 검증 행동 | 한마디로 |
|---|---|---|
| **V1** | 출처 확인 (Source Check) | 이 사이트 진짜야? |
| **V2** | 저자 확인 (Author Check) | 누가 썼고 사람 맞아? |
| **V3** | 콘텐츠 교차 확인 (Content Cross-check) | 다른 데서도 확인돼? |
| **V4** | 이미지·영상 확인 (Visual Verification) | 이 사진·영상 진짜야? |
| **V5** | 감정 반응 점검 (Emotional Reaction Check) | 나 너무 흥분한 거 아니야? (메타인지) |

> **V6(사용자 정의)**: 5대 검증 행동 어디에도 안 들어맞는 질문은 자동으로 V6으로 분류돼요. AI가 헷갈려서 임의로 끼워 맞추지 않게 하기 위함입니다. V6 항목은 보정·점수 계산엔 직접 영향을 안 주지만 여러분의 체크리스트엔 그대로 남아 있습니다.

> **V5는 왜 다른 4개랑 다를까?**
> V1~V4는 **자료를 보는** 행동이고, V5는 **자기 자신을 보는** 행동이에요. 강한 감정에 휩싸이면 우리 뇌는 비판적 사고를 잘 못 해요. 그래서 가짜뉴스를 만드는 사람들은 일부러 분노·공포를 자극해서 우리가 생각 없이 공유하게 만들어요. 이걸 인식하는 것 자체가 가장 강력한 방어막이에요.

---

## 3. 전체 흐름 한눈에 보기

```
[1] 내 체크리스트 만들기  ─────────────────►  질문마다 1~5점 루브릭 정의
                                                    │
                                                    ▼
[2] AI가 질문을 5대 검증 행동에 자동 매핑  ────►  "이 질문은 V3(콘텐츠 교차)이네"
                                                    │
                                                    ▼
[3] 교사가 등록한 미디어를 내가 평가
        + 같은 미디어를 교사도 평가
                  │
                  ▼ (교사 점수 − 내 점수 = "격차 gap")
[4] 항목별 보정값 계산
        : 검증 행동마다 gap의 "평균"을 보정값으로
                  │
                  ▼ 모델링이 쌓일수록 보정값이 참값에 수렴
                  │
                  ▼
[5] 새 미디어를 Gemini가 5대 검증 행동으로 1~5점 평가
        (V4는 본문에 시각 자료 언급이 없으면 N/A로 자동 제외)
                  │
                  ▼
[6] 최종 점수 = (AI 점수 + 보정값)의 평균을 50점 환산
        + 신뢰 등급 + (있다면) 과락 경고
                  │
                  ▼
[7] 결과 화면: 그대로 "수용" or 직접 점수 고쳐 "정교화"
                  │
                  ▼
[8] 검증 행동별 마스터리 갱신
        🌟 = 잘하는 행동, ⚠️ = 약점 행동
```

---

## 4. 6층 구조로 다시 풀면

```
Layer 6 │ 최종 점수(50점 만점) + 신뢰 등급 + 과락 경고 + 마스터리   ← 결과 카드
Layer 5 │ 교사와의 격차 분석 → 항목별 보정값 + 메타인지 피드백 카드
Layer 4 │ AI 점수에 보정값을 더해 50점으로 환산 (V4 N/A면 나머지로 평균)
Layer 3 │ Gemini가 5대 검증 행동에 1~5점 부여 (단일 호출)
Layer 2 │ 체크리스트 항목 → 5대 검증 행동 자동 매핑(캐싱)
Layer 1 │ 학생이 직접 만든 커스텀 체크리스트
```

> 위로 갈수록 "결과", 아래로 갈수록 "학생 본인의 입력"에 가깝습니다. 이 구조가 우리 모델의 이름 **VAPM (Verification Action-based Progressive fact-check Model)** 의 의미예요.
> - **Verification Action-based** = 5대 검증 행동에 정렬
> - **Progressive** = 모델링을 반복할수록 점진적으로 보정값과 마스터리가 정확해짐

---

## 5. 핵심 개념 4가지

### (1) 격차 (gap)

같은 미디어에 대해 **교사 점수 − 내 점수**.
- gap > 0 → 내가 너무 박하게 봤다 (교사가 더 높게 줌)
- gap < 0 → 내가 너무 후하게 봤다 (교사가 더 낮게 줌)

모델링을 저장할 때마다 검증 행동별 gap이 학습 데이터에 기록됩니다. (코드: `computeGap`)

### (2) 보정값 (correction)

지금까지 저장된 **모든 모델링 레코드**에서 검증 행동 V_i마다 gap의 평균을 낸 값이에요.

> `correction_i = clamp( mean(gap_i), −1.0, +1.0 )`   (그 항목 모델링 건수 ≥ 3)
> `correction_i = 0`                                   (건수 < 3)

말로 풀면:
- **"교사와 나의 채점이 평균 몇 점 차이 나는가"** 를 항목마다 구한 거예요.
- 예: 내가 V5(감정 반응)를 늘 후하게 줘서 gap 평균이 −0.6이면 → `correction_V5 = −0.6`.
- **누적이 아니라 매번 전량 다시 계산**합니다. 그래서 채점 순서가 달라도, 몇 번을 다시 계산해도 결과가 똑같아요(멱등).
- **±1.0점으로 제한**해요. 리커트(1~5점) 척도에서 한 눈금 이상 통째로 밀면 왜곡이 커지니까요.
- **3건 미만이면 보정하지 않아요.** 표본이 너무 적을 때 우연한 차이에 휘둘리는 걸 막기 위함이에요.
- 코드: `computeCorrections()` in `src/utils/hpfm.js`.

> **왜 곱셈(%)이 아니라 덧셈(±점수)인가요?** 리커트 척도는 "3점은 1점의 3배"처럼 비율로 계산하면 안 돼요. 게다가 곱셈 보정은 낮은 점수에서 계수가 폭발해버려요. 그래서 항상 **점수를 더하고 빼는** 방식만 씁니다.

### (3) 최종 점수 (50점 만점)

AI가 매긴 1~5점에 보정값을 더한 뒤 평균을 내서 50점으로 늘려요.

> 1단계  `corrected_i = clamp( aiScore_i + correction_i, 1, 5 )`   (aiScore가 N/A면 그대로 N/A)
> 2단계  `finalScore  = mean( corrected_i ) × 10`                 (10~50점, 소수 1자리)

- 가중치 없이 **단순 평균 × 10**이에요.
- V4가 N/A(이미지 없는 글)면 나머지 4개 행동의 평균으로 계산돼요(자동 재정규화).
- 유효 항목이 0개면 0점.
- 코드: `applyCorrections()`, `computeFinalScore()`.

### (4) 마스터리 (Mastery)

> `Mastery(V_i) = clamp( 1 − mean(|gap_i|) / 4, 0, 1 )`

각 검증 행동을 얼마나 안정적으로 수행하는지 0~1 사이 값으로 산출.

말로 풀면:
- 교사 기준과의 평균 격차가 작을수록 1에 가까워짐 = 그 검증 행동에 숙련됐다는 뜻.
- 해당 항목의 gap 데이터가 하나도 없으면 '데이터 없음'으로 표시돼요.

대시보드에서 🌟(80% 이상)/⚠️(40% 미만) 색상으로 잘하는 행동과 약점 행동이 한눈에 보여요.

---

## 6. 최종 점수와 등급

Gemini가 5대 검증 행동에 1~5점을 매기고, 거기에 보정값을 더해 50점으로 환산합니다(§5-(3)).

> 💡 **보정 예시 — 계산이 어떻게 되는지 확인하기**
> AI 점수 `{V1:4, V2:2, V3:5, V4:null, V5:4}`, 보정값 `{V1:0, V2:+1.0, V3:0, V4:0, V5:−0.6}` 이면
> · 보정 후: `{V1:4, V2:3, V3:5, V4:N/A, V5:3.4}`
> · 평균: (4 + 3 + 5 + 3.4) ÷ 4 = 3.85
> · 최종 점수: 3.85 × 10 = **38.5점** → 등급 "주의(caution)"
>
> **V4 N/A 사례**: V4가 빠지면 나머지 4개만 평균 내요. "이미지 없는 글이 불리해지는" 왜곡을 막기 위함이에요.

### 신뢰 등급 (band)

| 최종 점수 | 등급 | 의미 |
|---|---|---|
| **40점 이상** | 신뢰 높음 (high) | 신뢰도가 높은 미디어로 판단 |
| **30 ~ 40점** | 주의 (caution) | 일부 항목 확인 필요 |
| **20 ~ 30점** | 신뢰 낮음 (low) | 팩트체크 경고 |
| **20점 미만** | 매우 낮음 (veryLow) | 신뢰하기 어려움 |

> **컷 30점의 근거:** 전 항목 평균 3점('보통')에 해당하는 준거참조(criterion-referenced) 기준이에요. "몇 등인가"가 아니라 "기준을 얼마나 충족했는가"로 읽어요.

### 과락 규칙 (개별 항목 경고)

총점이 괜찮아도, **보정 후 어떤 항목이 2점 미만**이면 별도로 경고해요.

> 예: `{V1:1, V2:4, V3:4, V4:4, V5:4}` → 총점 34.0점(주의)이지만 **V1(출처)이 1점**이라
> "총점과 별개로 V1(출처) 확인이 심각하게 미흡" 경고 배지가 떠요.

- **왜 이중 기준인가?** 평균은 개별 결함을 가려버릴 수 있어요. 출처가 완전히 가짜여도 나머지가 좋으면 평균이 괜찮게 나오니까요. 그래서 "총점 등급 + 개별 과락"을 함께 봐요(교육측정의 표준적 이중 기준).
- 코드: `SCORE_BANDS`, `scoreBand()`, `DIMENSION_FLOOR`.

> **v3.0의 95% 신뢰구간(±오차범위)은 v4.0에 없어요.** 그건 σ(불확실성)에 의존했는데 σ 자체를 걷어냈기 때문이에요. 대신 "아직 보정 데이터가 적다(항목별 3건 미만)"는 사실을 결과 화면에 솔직하게 안내해요.

---

## 7. 보정은 언제 켜지나요? (보정 데이터가 적을 때)

항목별 모델링 건수가 **3건 미만이면** 그 항목은 보정 없이 AI 점수를 그대로 써요. **모든 항목이 3건 미만이면** 결과 화면에 이렇게 안내돼요:

> "아직 기준 다듬기 데이터가 적어 보정 없이 AI 점수를 그대로 계산했어요(항목별 3건 이상 필요)."

"기준 다듬기"에서 선생님 미디어를 3건 이상 채점하면 그 항목부터 보정이 켜집니다. (코드: `countAppliedCorrections()`, `MIN_CALIBRATION_COUNT`)

---

## 8. "수용" vs "정교화"

결과를 본 학생은 두 가지 선택을 합니다. **둘 다 보정값은 바꾸지 않아요** — 보정값은 교사 기준점이 있는 "기준 다듬기"에서만 바뀌어요. 수용/정교화는 마스터리·평가 습관 분석에만 쓰여요.

| 선택 | 학습 데이터에 기록되는 값 | 효과 |
|---|---|---|
| 🟢 **수용** | AI 점수 그대로 (`source: "accept"`) | 격차 ≈ 0으로 기록 → 마스터리·습관 분석 갱신 |
| 🟡 **정교화** | 학생이 직접 수정한 점수 (`source: "refine"`) | `학생−AI` 격차 기록 → 습관 분석에 반영 |

> "AI 결과에 동의하지 않는다"는 행위 자체가 **여러분의 비판적 판단**이에요. 그 판단은 여러분의 평가 습관 분석에 반영되지만, 교사 기준점이 없으므로 보정값 자체는 바꾸지 않아요.

---

## 9. 메타인지 피드백 카드

여러 번의 격차 패턴을 분석해서 자동으로 만드는 진단 카드입니다 (`generateFeedbackCards`).

| 카드 | 트리거 조건 | 의미 |
|---|---|---|
| **Over** | 평균 gap < −0.5 | 교사보다 평균 0.5점 이상 **후하게** 평가하는 경향 |
| **Under** | 평균 gap > +0.5 | 교사보다 평균 0.5점 이상 **박하게** 평가하는 경향 |
| **Inconsistent** | gap 분산 > 1.0 | 평가 결과가 **흔들리는** 경향 (기준이 일관되지 않음) |

각 카드에는 검증 행동 · 진단 · 자기 점검 가이드 · 구체적 개선 행동이 표시돼요.

> **메타인지(metacognition)** 란 "내가 어떻게 생각하고 있는지를 생각하는 능력"을 말합니다. 이 카드는 여러분 자신의 평가 습관을 거울처럼 비춰주는 도구예요.

---

## 10. 왜 이 방식이 타당한가요? (채점자 보정)

학생이 만든 체크리스트에는 교사 기준 대비 **체계적인 편차**가 있어요(누군가는 늘 후하고, 누군가는 늘 박하죠). AI는 그 학생의 체크리스트를 기준으로 채점하므로 **같은 편차를 물려받는다**고 볼 수 있어요.

그래서 "같은 미디어를 교사·학생이 채점한 평균 편차"를 측정해 두었다가 AI 점수에 그만큼 가감하면, AI 점수를 **교사 기준점에 정박**시킬 수 있어요. 이건 교육측정에서 오래 쓰인 **채점자 보정(rater calibration) / 조정(moderation)** 절차와 정확히 같은 논리예요.

- **v3.0(가중치 학습)과의 차이:** 예전엔 "어떤 행동을 더 중요하게 볼지(가중치)"를 경사하강으로 학습했어요. 지금은 가중치를 없애고 "교사와의 평균 점수 차이(보정)"만 씁니다. 더 단순하고, 왜 그 점수가 나왔는지 설명하기 쉬워요.

---

## 11. V4(이미지·영상 확인) N/A 자동 처리

본문에 사진·영상·그래프·차트·스크린샷 언급이 전혀 없는 텍스트 자료는 V4를 평가할 단서가 없어요. 그래서:

1. Gemini 프록시가 본문을 살펴보고 시각 자료 단서가 없으면 V4를 `score: null, skipped: true`로 반환합니다.
2. `applyCorrections`는 null인 V4에 보정을 더하지 않고 null로 유지합니다.
3. `computeFinalScore`는 V4를 빼고 나머지 4개 행동의 평균으로 점수를 냅니다.
4. 결과 화면 V4 카드는 "해당 없음 (N/A)"으로 표시돼요.
5. 보정값·마스터리 산출에서도 V4는 자동으로 제외됩니다.

본문에 시각 자료 언급이 조금이라도 있으면(예: "위 그래프", "사진에 보이는") V4도 정상 평가돼요. 학생이 실제 이미지를 첨부하면 AI가 그 이미지를 직접 분석해 반드시 점수를 줍니다.

---

## 12. 미디어 유형별 권장 가중치 프리셋 (참고용)

자료 유형에 따라 어떤 검증 행동이 더 중요한지 다릅니다. 코드에 5종 프리셋이 참고용으로 내장돼 있어요 (`MEDIA_TYPE_PRESETS` in `src/utils/hpfm.js`). 이 값은 체크리스트를 설계할 때 "어디에 질문을 더 둘지" 참고하는 용도예요(점수 계산에는 가중치를 쓰지 않아요).

| 미디어 유형 | V1 | V2 | V3 | V4 | V5 | 강조점 |
|------------|-----|-----|-----|-----|-----|---------|
| 뉴스 기사 | 0.20 | 0.15 | **0.30** | 0.15 | 0.20 | 교차 검증 우선 |
| SNS 게시물 | 0.15 | **0.30** | 0.15 | 0.20 | 0.20 | 봇·익명 점검 우선 |
| 영상 콘텐츠 | 0.15 | 0.20 | 0.20 | **0.30** | 0.15 | 딥페이크·이미지 우선 |
| 광고/홍보 | 0.20 | 0.15 | 0.20 | 0.15 | **0.30** | 감정 자극 우선 |
| 정부 공식 발표 | **0.30** | 0.25 | 0.25 | 0.10 | 0.10 | 출처 정확성 우선 |

---

## 13. 한 번의 평가, 어떻게 굴러갈까? (예시 시나리오)

**상황**: 학생이 감정 반응(V5)을 늘 후하게 준다고 가정. 모델링 4회 완료.

1. 학생이 미디어 A를 자기 체크리스트로 평가 → 검증 행동별 점수 집계 → `V1=4, V5=4, V4=N/A(이미지 없음), ...`
2. 교사도 미디어 A를 평가 → `V1=4, V5=3, ...`
3. **gap = 교사 − 학생** → `V5 = 3 − 4 = −1 (학생이 너무 후함)`
4. 모델링 4건의 V5 gap이 `[−0.5, −1.0, −0.5, −0.4]` → 평균 −0.6 → `correction_V5 = −0.6`
5. (V4는 모델링 2건뿐이라 보정 안 켜짐 → `correction_V4 = 0`)
6. 마스터리 산출: `Mastery(V_i) = 1 − |평균 gap_i|/4`
7. 누적된 격차들을 보고 "Over V5" 같은 피드백 카드 자동 생성
8. 새 미디어 B를 Gemini로 평가 → V5에 4점 → 보정 후 4 + (−0.6) = 3.4점 → 5개 평균 × 10 = **최종 점수**
9. 학생이 결과를 보고 "수용" 또는 "정교화" 선택 (보정값은 안 바뀜, 습관 분석만 갱신)

---

## 14. 자주 묻는 질문

**Q. AI(Gemini)가 틀린 점수를 주면 어떻게 되나요?**
A. "정교화" 버튼으로 점수를 직접 수정하세요. 그 차이는 여러분의 평가 습관 분석에 반영돼요. 다만 보정값 자체는 "기준 다듬기"에서 선생님과 비교할 때만 바뀝니다.

**Q. 처음엔 점수에 보정이 안 붙어요.**
A. 정상입니다. 항목별 모델링이 3건 이상 쌓여야 그 항목에 보정이 켜져요. 그 전까지는 AI 점수를 그대로 씁니다(과적합 방지).

**Q. 보정값이 계속 −1.0이나 +1.0에 붙어 있어요.**
A. 교사와 여러분의 채점 차이가 평균 1점을 넘는다는 뜻이에요. 보정은 ±1.0점으로 제한되니, 그만큼 채점 습관 차이가 크다는 신호예요. 피드백 카드를 참고해 그 검증 행동을 다시 점검해보세요.

**Q. 5대 검증 행동 중에 못 들어가는 질문이 있어요.**
A. AI가 자동으로 V6(사용자 정의)로 분류합니다. V6 항목은 점수·보정 계산엔 직접 영향을 안 주지만 여러분의 체크리스트엔 그대로 남아 있어요.

**Q. 왜 예전의 신뢰구간(±오차범위)이 사라졌어요?**
A. 신뢰구간은 σ(불확실성)로 계산했는데, v4.0에서 σ를 없앴기 때문이에요. 대신 "보정 데이터가 아직 적다"는 사실을 결과 화면에 직접 안내해요.

**Q. 마스터리가 뭐예요?**
A. 각 검증 행동에서 교사와 얼마나 가깝게 채점하는지를 0~1로 보여주는 점수예요. 교사와의 평균 격차가 작을수록 올라갑니다. 🌟(80% 이상)는 잘하는 행동, ⚠️(40% 미만)는 더 연습이 필요한 약점 행동이에요.

**Q. 왜 IFCN 강령에서 5대 검증 행동으로 바뀌었어요?**
A. IFCN 강령은 본래 **팩트체커 조직이 지켜야 할 윤리 기준**이지, 학생이 미디어 자료를 평가할 때 직접 수행하는 행동이 아니에요. VAPM은 "이렇게 행동해보자"라는 구체적 가이드(출처/저자/콘텐츠/이미지/감정)로 재설계해서 미디어 리터러시 교육에 더 적합하게 만든 거예요. 이전 7차원·5차원 데이터는 자동 변환되어 옮겨집니다.

---

## 15. 더 알아보기 (출처·참고문헌)

이 알고리즘은 **5대 검증 행동(SIFT 모델 계열)** 과 **채점자 보정(rater calibration)** 을 결합했습니다.

### 미디어 리터러시 프레임워크 (메인 표준)
- **SIFT (The Four Moves)**: Mike Caulfield, *SIFT (The Four Moves)*, 2019. Stop / Investigate / Find / Trace.
  공식 글: <https://hapgood.us/2019/06/19/sift-the-four-moves/>
  → V5(Stop), V1·V2(Investigate), V3(Find), V4(Trace) 행동에 영감을 줌.
- **한국언론진흥재단 미디어 리터러시 교육 자료**: 디지털 환경에서의 검증 행동 가이드.
- **EU vs Disinformation / EUFactCheck**: 유럽위원회 산하 디지털 시민 역량 가이드. 봇·딥페이크·역이미지 검색 등 디지털 환경 위협에 대한 점검 행동을 권장.

### 보조 프레임워크 (체크리스트 설계 시 참고)
- **CRAAP Test**: Sarah Blakeslee, *The CRAAP Test*, LOEX Quarterly 31(3), 2004. 정보 평가의 5요소(Currency, Relevance, Authority, Accuracy, Purpose).
- **IFCN Code of Principles**: International Fact-Checking Network (Poynter Institute). 본 모델 v2.0의 토대였으며, V1·V2(출처·저자 투명성)는 IFCN 강령 1·3과 정신을 공유합니다.

### 교육측정 (v4.0의 채점 방식)
- **Rater Calibration / Moderation**: 교육측정에서 서로 다른 채점자의 체계적 편차를 기준 채점자(anchor)에 맞춰 보정하는 표준 절차. 본 모델의 "보정값 = 교사·학생 평균 차이"가 여기에 해당.
- **Criterion-referenced Assessment**: 상대 순위가 아니라 준거(기준) 충족도로 평가하는 방식. 30점 컷(전 항목 평균 3점)과 개별 과락(2점 미만)의 근거.

### AI / LLM
- **Google Gemini API**: 본 앱의 5대 검증 행동 평가는 Gemini 2.5 Flash 모델을 Netlify Function 프록시로 호출.
  공식 문서: <https://ai.google.dev/gemini-api/docs>

### 디지털 검증 도구 (V4 관련)
- **Google 이미지 역검색**: 이미지 우클릭 → "Google에서 이미지 검색"
- **TinEye**: <https://tineye.com/> — 이미지 최초 게시 시점 추적
- **InVID Verification Plugin**: 영상 검증 브라우저 확장
- **Forensically**: <https://29a.ch/photo-forensics> — 이미지 조작 분석

### 코드에서 직접 확인하기
- 알고리즘 본체: `src/utils/hpfm.js` (파일명은 v1~v3 호환 유지, 모델은 VAPM-4.0)
- 보정값 산출: `computeCorrections` in `src/utils/hpfm.js`
- 보정 적용·50점 환산·등급/과락: `applyCorrections`, `computeFinalScore`, `scoreBand` in `src/utils/hpfm.js`
- 레거시 → v3 차원 마이그레이션 헬퍼: `migrateLegacyDimensionScores` in `src/utils/hpfm.js`
- 3.0 → 4.0 모델 레이지 마이그레이션: `getAlgorithmModel` in `src/services/firestore.js`
- AI 호출(매핑/평가): `netlify/functions/gemini.js`
- 마스터리 산출: `computeMastery` in `src/utils/hpfm.js`

---

## 16. 학습 활동 아이디어 (선생님과 함께)

1. **같은 기사, 다른 체크리스트**: 모둠별로 다른 검증 행동을 강조하는 체크리스트를 만든 뒤, 같은 기사에 대해 어떤 결과 차이가 나오는지 비교.
2. **보정값 변화 추적**: 모델링을 3회, 5회, 10회 했을 때 각 검증 행동(V1~V5)의 보정값이 어떻게 안정되는지 표로 그려보기.
3. **AI vs 인간**: Gemini의 5대 검증 행동 평가를 그대로 수용한 결과와, 정교화로 수정한 결과를 비교해 "AI를 어디까지 신뢰할 것인가" 토론.
4. **메타인지 일지**: 피드백 카드(Over/Under/Inconsistent)를 받으면, 그 원인을 자기 언어로 정리하고 다음 평가에서 어떻게 다르게 해볼지 미리 적기.
5. **과락의 의미**: 총점은 높은데 특정 항목이 과락(2점 미만)인 사례를 찾아, "평균이 결함을 가리는" 상황을 직접 토론.
6. **검증 행동 시연**: 교사가 학생들 앞에서 도메인 검색(V1) → 작성자 구글링(V2) → 다른 매체 찾기(V3) → 역이미지 검색(V4) → 자기 감정 점검(V5)을 실제로 시연.
7. **마스터리 진단**: 학기 중 마스터리 그래프를 보고 가장 약한 검증 행동을 집중 학습. 학기 말에 다시 비교.

---

> 이 문서는 학생용 학습 보조 자료입니다. 알고리즘의 기술적 설치·배포 안내는 [README.md](./README.md)를 참고하세요.
